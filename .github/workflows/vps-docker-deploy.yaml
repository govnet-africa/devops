name: Deploy to docker host
  
on: 
  workflow_dispatch:
  workflow_call:
    inputs:
      service:
        required: true
        type: string
      tag:
        required: false
        type: string
        default: ${{ github.sha }}
      deploy-host:
        required: false
        type: string
        default: "${{ inputs.service }}.ifas.mofa"
      deploy-host-ip:
        required: false
        type: string
        default: '109.74.200.222'
      deploy-user:
        required: false
        type: string
        default: 'github-actions'
      deploy-dir:
        required: false
        type: string
        default: '/deploy/mofa-ifas'
jobs:
  docker_deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write
      contents: read
    steps:
      - name: Prep shared variables
        run: |
          echo "DEPLOY_HOST_KEYSCAN=`ssh-keyscan ${{ inputs.deploy-host-ip }}`" >> $GITHUB_ENV
      - name: Configure Host SSH
        uses: shimataro/ssh-key-action@v2
        with:
          name: ifas_github
          known_hosts: ${{ env.DEPLOY_HOST_KEYSCAN }}
          config: |
            Host ${{ inputs.deploy-host }}
              HostName ${{ inputs.deploy-host-ip }}
              User ${{ inputs.deploy-user }}
              IdentityFile ~/.ssh/ifas_github
          key: |
            -----BEGIN OPENSSH PRIVATE KEY-----
            
            -----END OPENSSH PRIVATE KEY-----
      - name: Deploy ${{ inputs.service }} with docker
        run: ssh ${{ inputs.deploy-host }} "make -C  ${{ inputs.deploy-dir }} deploy-service-tag service=${{ inputs.service }} tag=${{ inputs.tag }}"